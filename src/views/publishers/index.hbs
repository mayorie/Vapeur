<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Vapeur - Éditeurs</title>

  <style>
    /* RESET */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #1b1b1b;
      color: #f1f1f1;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* HEADER */
    header {
      background-color: #171a21;
      padding: 20px;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.5);
    }

    header h1 {
      font-size: 2.2rem;
      color: #66c0f4;
    }

    /* CONTENEUR PRINCIPAL */
    .editeurs-page {
      max-width: 1000px;
      margin: 60px auto;
      padding: 0 20px;
      font-family: Arial, sans-serif;
    }

    /* TITRE */
    .editeurs-page h1 {
      font-size: 2.5rem;
      color: #66c0f4;
      margin-bottom: 25px;
    }

    /* ALERT SUCCESS */
    .alert-success {
      background-color: #2a3f2a;
      color: #a8d08d;
      padding: 15px 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.3);
    }

    /* BOUTON AJOUTER */
    .editeurs-page a.add-button {
      display: inline-block;
      margin-bottom: 25px;
      padding: 10px 20px;
      background-color: #66c0f4;
      color: #0d0d14;
      border-radius: 8px;
      font-weight: bold;
      transition: background-color 0.3s, transform 0.2s;
      text-decoration: none;
    }

    .editeurs-page a.add-button:hover {
      background-color: #8fd3ff;
      transform: translateY(-2px);
    }

    /* LISTE DES ÉDITEURS */
    .editeurs-page ul {
      list-style: none;
      padding: 0;
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
    }

    .editeurs-page li {
      background-color: #20232a;
      padding: 15px 20px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.4);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .editeurs-page li:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.6);
    }

    .editeurs-page li a {
      color: #66c0f4;
      font-weight: bold;
      font-size: 1.1rem;
      text-decoration: none;
    }

    .editeurs-page li a:hover {
      text-decoration: underline;
    }

    /* MESSAGE VIDE */
    .editeurs-page .empty-message {
      font-size: 1.1rem;
      color: #aaa;
      background-color: #20232a;
      padding: 20px;
      border-radius: 10px;
      text-align: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.4);
      margin-top: 20px;
    }

    /* BOUTON RETOUR */
    .back-button {
      margin: 25px 0;
      padding: 10px 18px;
      background-color: #b22222; /* rouge mat */
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: bold;
      box-shadow: 0 4px 8px rgba(0,0,0,0.4);
      transition: background-color 0.3s, transform 0.2s;
      display: block;
    }

    .back-button:hover {
      background-color: #cc3333;
      transform: translateY(-2px);
    }

    .back-button:active {
      transform: translateY(0);
    }

    /* FOOTER */
    footer {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background-color: #171a21;
      text-align: center;
      padding: 15px 0;
      color: #888;
      font-size: 0.9rem;
      z-index: 1000;
    }
  </style>
</head>

<body>

<header>
  <h1>Vapeur</h1>
</header>

<section class="editeurs-page">

  <h1>Éditeurs</h1>

  {{#if query.success}}
    <div class="alert alert-success">
      {{#if (eq query.success 'delete')}}
        Éditeur supprimé avec succès !
      {{/if}}
    </div>
  {{/if}}

  <p>
    <a href="/editeurs/new" class="add-button">+ Ajouter un éditeur</a>
  </p>

  {{#if editeurs.length}}
    <ul>
      {{#each editeurs}}
        <li>
          <a href="/editeurs/{{this.id}}">{{this.nom}}</a>
        </li>
      {{/each}}
    </ul>
  {{else}}
    <p class="empty-message">Il n'y a pas encore d'éditeurs.</p>
  {{/if}}

  <a href="/" class="back-button">← Retour à la liste des éditeurs</a>

</section>

<footer>
  &copy; 2025 Vapeur - Tous droits réservés
</footer>

</body>
</html>
