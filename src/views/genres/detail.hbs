<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Vapeur - {{genre.nom}}</title>

  <style>
    /* RESET */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #1b1b1b;
      color: #f1f1f1;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* HEADER */
    header {
      background-color: #171a21;
      padding: 20px;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.5);
    }

    header h1 {
      font-size: 2.2rem;
      color: #66c0f4;
    }

    /* CONTENEUR PRINCIPAL */
    .genre-page {
      max-width: 1200px;
      margin: 60px auto;
      padding: 0 20px;
    }

    /* TITRES */
    .genre-title {
      font-size: 2.5rem;
      color: #66c0f4;
      margin-bottom: 15px;
    }

    .genre-subtitle {
      font-size: 1.6rem;
      margin-bottom: 30px;
      color: #d0d0d0;
    }

    /* LISTE DES JEUX */
    .games-list {
      list-style: none;
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
    }

    .game-item {
      background-color: #20232a;
      border-radius: 10px;
      padding: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.4);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .game-item:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.6);
    }

    /* IMAGE DU JEU */
    .game-image {
      width: 100%;
      height: 180px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 10px;
      display: block;
    }

    .game-title {
      display: block;
      font-size: 1.2rem;
      font-weight: bold;
      color: #66c0f4;
      margin-bottom: 5px;
    }

    .game-link:hover .game-title {
      text-decoration: underline;
    }

    .game-editor {
      font-size: 0.95rem;
      color: #aaa;
    }

    /* MESSAGE VIDE */
    .empty-message {
      font-size: 1.1rem;
      color: #aaa;
      background-color: #20232a;
      padding: 20px;
      border-radius: 10px;
      text-align: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.4);
    }

    /* BOUTON RETOUR */
    .back-button {
      margin: 20px 0;
      padding: 10px 18px;
      background-color: #b22222; /* rouge mat */
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: bold;
      box-shadow: 0 4px 8px rgba(0,0,0,0.4);
      transition: background-color 0.3s, transform 0.2s;
    }

    .back-button:hover {
      background-color: #cc3333;
      transform: translateY(-2px);
    }

    .back-button:active {
      transform: translateY(0);
    }

    /* FOOTER */
    footer {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background-color: #171a21;
      text-align: center;
      padding: 15px 0;
      color: #888;
      font-size: 0.9rem;
      z-index: 1000;
    }
  </style>
</head>

<body>

<header>
  <h1>Vapeur</h1>
</header>

<section class="genre-page">

  <h1 class="genre-title">{{genre.nom}}</h1>
  <h2 class="genre-subtitle">Jeux de ce genre</h2>

  {{#if genre.jeux.length}}
    <ul class="games-list">
      {{#each genre.jeux}}
        <li class="game-item">
          <a href="/games/{{this.jeu.id}}" class="game-link">
            {{#if this.jeu.lien}}
              <img src="{{this.jeu.lien}}" alt="{{this.jeu.titre}}" class="game-image">
            {{/if}}
            <span class="game-title">{{this.jeu.titre}}</span>
          </a>

          {{#if this.jeu.editeur}}
            <span class="game-editor">{{this.jeu.editeur.nom}}</span>
          {{/if}}
        </li>
      {{/each}}
    </ul>
  {{else}}
    <p class="empty-message">
      Il n'y a pas encore de jeux pour ce genre.
    </p>
  {{/if}}

  <button onclick="history.back()" class="back-button">← Retour</button>

</section>

<footer>
  &copy; 2025 Vapeur - Tous droits réservés
</footer>

</body>
</html>
