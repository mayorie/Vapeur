<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Vapeur - Tous les jeux</title>

  <style>
    /* RESET */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #1b1b1b;
      color: #f1f1f1;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    button {
      cursor: pointer;
    }

    /* HEADER */
    header {
      background-color: #171a21;
      padding: 20px;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.5);
    }

    header h1 {
      font-size: 2.5rem;
      color: #66c0f4;
    }

    /* CONTENU PRINCIPAL */
    main {
      max-width: 1200px;
      margin: 40px auto;
      padding: 0 20px;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 20px;
      color: #66c0f4;
    }

    #games-list {
      list-style: none;
    }

    #games-list li {
      background-color: #171a21;
      padding: 15px 20px;
      margin-bottom: 15px;
      border-radius: 10px;
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      box-shadow: 0 2px 5px rgba(0,0,0,0.5);
    }

    .game-info {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .game-info img {
      width: 80px;
      height: 80px;
      object-fit: cover;
      border-radius: 8px;
      flex-shrink: 0;
    }

    .game-details {
      display: flex;
      flex-direction: column;
    }

    .game-details a {
      color: #f1f1f1;
      font-weight: bold;
      font-size: 1.1rem;
    }

    .genres {
      font-size: 0.9rem;
      color: #aaa;
    }

    .edit-btn, .delete-btn {
      background-color: #66c0f4;
      color: #0d0d14;
      border: none;
      padding: 8px 15px;
      border-radius: 5px;
      font-weight: bold;
      margin-left: 10px;
      transition: background-color 0.3s, transform 0.2s;
    }

    .edit-btn:hover, .delete-btn:hover {
      background-color: #8fd3ff;
      transform: translateY(-2px);
    }

    .add-game {
      display: inline-block;
      margin-bottom: 20px;
      background-color: #66c0f4;
      color: #0d0d14;
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: bold;
      transition: background-color 0.3s, transform 0.2s;
    }

    .add-game:hover {
      background-color: #8fd3ff;
      transform: translateY(-2px);
    }

    /* FOOTER */
    footer {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background-color: #171a21;
      text-align: center;
      padding: 15px 0;
      color: #888;
      font-size: 0.9rem;
      z-index: 1000;
    }
    .back-button {
      margin: 25px 400px;
      padding: 10px 18px;
      background-color: #b22222; /* rouge mat */
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: bold;
      box-shadow: 0 4px 8px rgba(0,0,0,0.4);
      transition: background-color 0.3s, transform 0.2s;
      display: block;
    }

    .back-button:hover {
      background-color: #cc3333;
      transform: translateY(-2px);
    }

    .back-button:active {
      transform: translateY(0);
    }
  </style>
</head>

<body>

<header>
  <h1>Vapeur</h1>
</header>

<main>
  <h1>Tous les jeux</h1>

  <p>
    <a class="add-game" href="/games/new">+ Ajouter un jeu</a>
  </p>

  {{#if games.length}}
    <ul id="games-list">
      {{#each games}}
        <li data-id="{{this.id}}">
          <div class="game-info">
            <img src="{{this.lien}}" alt="{{this.titre}}">
            <div class="game-details">
              <a href="/games/{{this.id}}">{{this.titre}}</a>
              <span>{{this.editeur.nom}} — 
                <span class="genres">
                  {{#each this.genres}}
                    {{this.genre.nom}}{{#unless @last}}, {{/unless}}
                  {{/each}}
                </span>
              </span>
            </div>
          </div>

          <div>
            <a href="/games/new/{{this.id}}">
              <button class="edit-btn">Modifier</button>
            </a>
            <button class="delete-btn">Supprimer</button>
          </div>
        </li>
      {{/each}}
    </ul>
  {{else}}
    <p>Il n'y a pas encore de jeux.</p>
  {{/if}}
</main>

<a href="/" class="back-button">← Retour au Menu Principal</a>

<footer>
  &copy; 2025 Vapeur - Tous droits réservés
</footer>

<script>
  document.querySelectorAll('.delete-btn').forEach(button => {
    button.addEventListener('click', async () => {
      const li = button.closest('li');
      const id = li.dataset.id;

      if (!confirm('Voulez-vous vraiment supprimer ce jeu ?')) return;

      try {
        const response = await fetch(`/games/${id}`, {
          method: 'DELETE',
        });

        const result = await response.json();
        if (result.success) {
          li.remove();
        } else {
          alert('Erreur lors de la suppression du jeu');
        }
      } catch (err) {
        console.error(err);
        alert('Erreur lors de la suppression du jeu');
      }
    });
  });
</script>


</body>
</html>
